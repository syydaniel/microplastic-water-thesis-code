# Core dependencies for the full thesis pipeline (Ch2 + Ch3 + Ch4).
# ASReview (Ch2 Step 1.ii) is run interactively in its own environment;
# install separately per https://asreview.readthedocs.io/.

# === Common ===
pandas>=2.0
numpy>=1.26
openpyxl>=3.1
tqdm>=4.66

# === Ch2 — LLM screening + data extraction ===
google-generativeai>=0.8
PyPDF2>=3.0

# === Ch3 — LightGBM + SHAP + Optuna + cross-validation ===
lightgbm>=4.6
shap>=0.51
optuna>=4.0
scikit-learn>=1.4
joblib>=1.3
scipy>=1.11

# === Ch3 / Ch4 — spatial + visualisation ===
geopandas>=1.0
shapely>=2.0
fiona>=1.9
rasterio>=1.3
pyproj>=3.6
matplotlib>=3.8
cartopy>=0.23
